---
"@tdreyno/he-said": minor
---

`collectRuleTerms(rule)` reports every term and fact a rule references (normalized to roots, facts separated since they must be bound) — so consumers can assert environment completeness explicitly instead of discovering a silent free join.
