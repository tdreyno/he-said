---
"@tdreyno/he-said": patch
---

docs(rebac): document ownership-rule reuse via `resourceType().ownedBy()`

Records FR-20 closure by documenting the shipped ownership export path:
`resourceType(...).ownedBy(scopeTerm)`.
