---
"@tdreyno/he-said": minor
---

`explainAllow(engine, rule, env)` — the allow-side twin of deny proofs: re-evaluates OR alternatives in order and reports the winning choices (outermost first), labeled from rule annotations or structural summaries via the new `summarizeRule`. Answers "why did this actor get in" for audits and support without reading SQL.
