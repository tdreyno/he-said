---
"@tdreyno/he-said": patch
---

ReBAC path intermediates are labeled from the next hop's source table ("via branches") instead of positional "rebac.path.N", so proofs and mermaid diagrams name every variable in an ownership chain. Unattributed relations keep the positional fallback.
